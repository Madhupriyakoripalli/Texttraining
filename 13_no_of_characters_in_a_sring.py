# create a string from the user
user_string = input("Enter a string: ")
# Calculate the number of characters in the string
num_characters = len(user_string)
# Print the user-entered string
print("String entered by the user:", user_string)
# Print the number of characters in the string
print("Number of characters in the string:", num_characters)
